// export async function createContext({ req, res }: { req: Request, res: Response }) {
//     const session = console.log(req, res);
//     if (!session || typeof session === "undefined") return {}

//     const { user, accessToken } = session;

//     return {
//         user,
//         accessToken,
//     }
// }